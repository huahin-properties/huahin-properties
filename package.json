{
  "name": "huahin-properties",
  "private": true,
  "description": "Dev-only tooling for Firestore emulator security tests (Conversation Ownership). Not part of the deployed site.",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "mocha": "^10.4.0"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"mocha tests/conversations.rules.test.js --timeout 20000\""
  }
}
